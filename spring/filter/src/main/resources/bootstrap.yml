spring:
  application:
    name: application-service
  cloud:
    consul:
      host: ${HOST_IP:localhost}
      port: 8500
      config:
        enabled: true
        failFast: false
      discovery:
        hostname: ${HOST_IP:localhost}
        register-health-check: false

