apiVersion: v1
kind: Service
metadata:
  name: filter-service
  labels:
    run: filter-deployment
spec:
  ports:
    - protocol: TCP
      port: 8888
      targetPort: 8080
  selector:
    run: filter-deployment
